#!/bin/bash
source {{ venv_otcws }}/bin/activate || exit
export CPPFLAGS="-DMINIMAL_NODE_NAVIGATION_PTRS"
export OTROOT="{{ ws_dir }}"
echo "CPPFLAGS=${CPPFLAGS}"
echo "CXXFLAGS=${CXXFLAGS}"
echo "LDFLAGS=${LDFLAGS}"
meson build ../source --prefix="${OTROOT}/local" -Dwebservices="{{ otcws }}" -Drestbed_dir="${OTROOT}/local" -Dbuildtype=release
